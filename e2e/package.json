{
  "name": "tfgv3-e2e",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test:e2e": "playwright test",
    "install-playwright": "npx playwright install --with-deps"
  },
  "devDependencies": {
    "@playwright/test": "^1.41.0",
    "@axe-core/playwright": "^4.6.0"
  }
  ,
  "scripts": {
    "test:axe": "playwright test tests/accessibility.spec.ts --project=chromium --reporter=list"
  }
}
